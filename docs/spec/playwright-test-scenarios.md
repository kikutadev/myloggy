# Playwright テストシナリオ

## セットアップ

```bash
# Playwright インストール済み
pnpm add -D @playwright/test playwright
npx playwright install chromium
```

### playwright.config.ts の設定

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'pnpm dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
  },
});
```

## テストシナリオ

### 1. 表示切替 (Day/Week/Month)

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| V001 | デフォルトでDayビューが表示される | 日付形式のビューが表示される |
| V002 | 「Week」タブをクリック | 週ビューに切り替わる |
| V003 | 「Month」タブをクリック | 月ビューに切り替わる |
| V004 | 「Day」タブをクリック | 日ビューに戻る |

### 2. 日付ナビゲーション

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| N001 | 「←」ボタンをクリック | 前日のビューに移動 |
| N002 | 「→」ボタンをクリック | 翌日のビューに移動 |
| N003 | 「今日」ボタンをクリック | 今日のビューに移動 |

### 3. トラッキング操作

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| T001 | 「停止中」ボタンをクリック | トラッキングが開始される（ボタンが「記録中」に変わる） |
| T002 | 「記録中」ボタンをクリック | トラッキングが停止する（ボタンが「停止中」に変わる） |
| T003 | トラッキング中にチェックポイントが表示される | タイムラインにチェックポイントが表示される |

### 4. キャプチャ操作

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| C001 | 「今すぐ記録」ボタンをクリック | キャプチャが実行される |
| C002 | キャプチャ実行中 | ボタンが「記録中...」になりdisabledになる |

### 5. AI分析操作

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| A001 | 「今すぐAI処理」ボタンをクリック | 分析が実行される |
| A002 | 分析中に同じボタンをクリック | 無視される（重複実行防止） |
| A003 | 分析完了後、ワークログが更新される | カテゴリやサマリーが入力される |

### 6. 設定画面

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| S001 | 歯車アイコンをクリック | 設定モーダルが表示される |
| S002 | 言語を「日本語」から「英語」に変更 | UIの言語が英語に切り替わる |
| S003 | カテゴリを追加 | カテゴリリストに追加される |
| S004 | 除外アプリを追加 | 除外アプリリストに追加される |
| S005 | 「保存」ボタンをクリック | 設定が保存される |
| S006 | 「✕」ボタンをクリック | 設定モーダルが閉じる |
| S007 | モーダル外をクリック | 設定モーダルが閉じる |

### 7. LLM モデルテスト

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| L001 | 設定で「モデル確認」ボタンをクリック | テストが実行される |
| L002 | Ollamaが起動しており、モデルが応答する場合 | 「モデル応答OK」のメッセージが表示される |
| L003 | Ollamaが起動していない場合 | 「モデル確認失敗」のメッセージが表示される |

### 8. データ表示

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| D001 | カテゴリサマリーが表示される | カテゴリごとの時間がバーで表示される |
| D002 | プロジェクトサマリーが表示される | プロジェクトごとの時間が表示される |
| D003 | ワークログがない場合 | 「作業記録なし」のメッセージが表示される |

### 9. チェックポイント表示

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| P001 | ワークログにチェックポイントがある場合 | 「チェックポイント N件」が表示される |
| P002 | チェックポイントの明細を開く | チェックポイントリストが表示される |

### 10. エラー表示

| ID | シナリオ | 期待結果 |
|----|---------|---------|
| E001 | AI処理に失敗した場合 | エラーバーナーが表示される |
| E002 | 「デバッグ」ボタンをクリック | デバッグモーダルが表示される |
| E003 | 「エラーを消す」ボタンをクリック | エラーがクリアされる |

## テスト実行コマンド

```bash
# テスト実行
pnpm playwright test

# UI付きで実行
pnpm playwright test --ui

# 特定ファイルのみ実行
pnpm playwright test e2e/app.spec.ts

# 特定シナリオのみ実行
pnpm playwright test -g "V001"
```